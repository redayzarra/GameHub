import { UseStore } from 'zustand';
export declare function mountStoreDevtool<T extends object = Record<string | number | symbol, any>>(storeName: string, store: UseStore<T>, rootElement?: HTMLElement): void;
